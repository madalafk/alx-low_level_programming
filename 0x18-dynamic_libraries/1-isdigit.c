#include "main.h"

/**
 * _isdigit - checks if input par c is a digit or not
 * @c: input par int c
 * Return: true if c is int,1  or 2 if c in not int (false)
 */
int _isdigit(int c)
{
	return (c >= 48 && c < 58);
}
