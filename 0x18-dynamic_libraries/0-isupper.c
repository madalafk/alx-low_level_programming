#include "main.h"

/**
 * _isupper - checks if c is upper or lower
 * @c: the input character provided
 *
 * Description: this  check for uppercase letters based on the input
 * Return: 0 on success
 */

int _isupper(int c)
{
	return (c >= 65 && c <= 90);
}
